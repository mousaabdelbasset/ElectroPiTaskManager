# Layout — Functional Documentation

The application uses the supplied ElectroPi image logo on desktop and mobile. The sidebar has compact 44px navigation targets, and New Task has the same width and side edges as the navigation items in English and Arabic.

The header no longer contains a user/avatar circle. Desktop keeps the fixed sidebar; mobile keeps the existing menu overlay. Switching to Arabic mirrors the full layout without changing the logo's aspect ratio.
